# Base URL of the Mikhtav API. Copy to `.env` for local dev.
VITE_API_URL=http://localhost:5080

# Production (Vercel): set VITE_API_URL in the Vercel project settings, e.g.
# VITE_API_URL=https://mikhtav-api.azurewebsites.net
